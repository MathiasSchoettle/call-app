{
  "name": "voip-capacitor-prototype",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "nuxt dev",
    "build": "nuxt generate",
    "android:sync": "npm run build && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest",
    "nuxt": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest",
    "typescript": "latest"
  }
}
