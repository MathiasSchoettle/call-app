import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.example.voipprototype',
  appName: 'VoIP Prototype',
  webDir: '.output/public',
}

export default config
